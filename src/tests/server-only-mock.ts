// Stub untuk paket `server-only` saat menjalankan test (Vitest tidak
// menyetel kondisi resolusi `react-server` seperti Next.js, sehingga paket
// asli akan selalu melempar error jika diimpor apa adanya). Lihat alias di
// `vitest.config.ts`.
export {};
